name: use
description: A new Flutter project.

# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.18.6 <3.0.0"

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.2
  get: ^4.6.5
  firebase_core: ^2.4.1
  purchases_flutter: ^4.5.5
  settings_ui: ^2.0.2
  in_app_review: ^2.0.6
  flutter_native_splash: ^2.2.16
  shared_preferences: ^2.0.15
  flutter_chat_ui: ^1.6.6
  uuid: ^3.0.7
  path_provider: ^2.0.11
  open_filex: ^4.3.2
  mime: ^1.0.3
  image_picker: ^0.8.6
  file_picker: ^5.2.4
  firebase_analytics: ^10.1.0
  firebase_crashlytics: ^3.0.9
  firebase_remote_config: ^3.0.9
  flutter_email_sender: ^5.2.0
  webview_flutter: ^4.0.2
  sqflite: ^2.2.4+1
  provider: ^6.0.5
  package_info_plus: ^3.0.3
  cached_network_image: ^3.2.3
  share_extend: ^2.0.0
  image_downloader: ^0.31.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^2.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - resources/images/raw_logo.png
    - resources/images/onboarding_items/chatgpt.png
    - resources/images/onboarding_items/dalle.png
    - resources/images/onboarding_items/help_us_grow.png
    - resources/images/example_images/example_image_1.png
    - resources/images/example_images/example_image_2.png
    - resources/images/example_images/example_image_3.png
    - resources/images/example_images/example_image_4.png
    - resources/images/example_images/example_image_5.png
    - resources/images/example_images/example_image_6.png
    - resources/images/models/ada.png
    - resources/images/models/babbage.png
    - resources/images/models/curie.png
    - resources/images/models/davinci.png
    - resources/images/gradient_background.png
    - resources/images/offer_paywall_cover.png
    - resources/images/second_offer_paywall_cover.png
    - resources/images/paywall_cover.png
    - resources/images/tip_image.png
    - resources/images/rating_cover.png
    - resources/images/arrow_right.png

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages
